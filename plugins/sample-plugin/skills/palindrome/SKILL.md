---
name: palindrome
description: Reverses a word or phrase using a Python script
---

Execute the palindrome.py script with the word or phrase provided by the user. If no word is provided, ask the user what word they'd like to reverse.

Run the script using:
```bash
python .claude/skills/palindrome/palindrome.py <ARGUMENTS>
```

Replace <ARGUMENTS> with the user's input.
